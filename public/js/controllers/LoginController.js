angular.module('arrumaqui')

.controller('LoginController', function($scope, LoginService) {
    
    $scope.login.email = '';
    $scope.login.senha = '';

    $scope.login = () => {
        
    }
});